<div class="logo">three-hundred and sixty five</div><br>
<br>
<div class="rule"></div>

<% Day.getting_dates.each do |single_day, font| %>
  <div id="ampersandness" onclick="fontLink('https://github.com/leahjs/ampersancy');" >
  <div class="day1" style="font-family:<%= font %>">&</div>
  <div class="dayoftheweek"><%= single_day.strftime('%A').upcase %></div>
  <div class="date"><%= single_day.strftime('%-d') %></div>
  <div class="monyear"><%= single_day.strftime('%B, %Y') %></div><br> 
  </div>
<div class="rule"></div>
<% end %>

<!-- <p id="notice"><%#= notice %></p>

<div><h1>Listing Days</h1></div>

<table>
  <thead>
    <tr>
      <th>Ampersand</th>
      <th>Weekday</th>
      <th>Date</th>
      <th>Month</th>
      <th>Year</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <#% @days.each do |day| %>
      <tr>
        <td><#%= day.ampersand %></td>
        <td><#%= day.weekday %></td>
        <td><#%= day.date %></td>
        <td><#%= day.month %></td>
        <td><#%= day.year %></td>
        <td><#%= link_to 'Show', day %></td>
        <td><#%= link_to 'Edit', edit_day_path(day) %></td>
        <td><#%= link_to 'Destroy', day, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <#% end %>
  </tbody>
</table>

<br> -->

<%#= link_to 'New Day', new_day_path %>
